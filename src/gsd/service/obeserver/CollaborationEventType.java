package gsd.service.obeserver;

/**
 * Enumeration class for collaboration event types
 */
public enum CollaborationEventType {
	DOCTOR, // when a doctor is the instigator for collaboration
	IMAGE, // when a image selection is the instigator for collaboration
	END
	// when the image collaboration is closed/ended
}
